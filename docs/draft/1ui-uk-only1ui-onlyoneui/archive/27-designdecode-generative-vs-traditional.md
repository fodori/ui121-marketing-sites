# Design Decade — Generative UI vs traditional UI design

- **Source:** https://designdecode.org/generative-ui-vs-traditional-ui-design-when-should-you-use-each/
- **Fetched:** 2026-09-23
- **Status:** failed

## Summary

The URL does not resolve to an article. A request on 23 September 2026 returned HTTP 404. The host redirects (WordPress, 301) from `designdecode.org` to `www.designdecode.org` and the same path is still 404. Nearby slugs (`/generative-ui-vs-traditional-ui-design/` and `/generative-ui-vs-traditional-ui/`) are also 404. A site search for “generative UI” on Design Decode returned unrelated magazine interviews, not a comparison of generative and traditional UI.

No thesis, statistics, product mechanism, or section outline could be taken from the page because there is no page. SOURCES.md describes the intended piece as a definition of generative UI (an interface generated at runtime by an agent from the request and context) compared with traditional design. That description is a pointer only and is not treated as the article.

## Key facts

- Fetched URL status: 404 after redirect to the www host.
- Variant slugs checked the same day: 404.
- Site search did not surface a matching title.
- No license, author, or date could be recorded from the target.

## How it works

Not available. The comparison SOURCES.md expected — when to generate UI at runtime versus when to design it ahead of time — is not on this URL.

## Implications for the one-UI domains / ui121

Do not cite Design Decode for a generative-versus-traditional framework until a live URL is found. The same distinction is covered, with fetchable text, by Thesys (sources 25 and the companion note), the Medium AG-UI piece (source 26), and Lazarev’s paradigm map (source 33). Those notes already support the one-UI line: stable tasks stay on a governed canvas; variable, context-specific tasks are composed from the same blocks rather than from a new app or a chat transcript.
